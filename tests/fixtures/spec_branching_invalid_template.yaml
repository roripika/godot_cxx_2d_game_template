# branching_invalid_template fixture (GTC-02)
# template 不一致で generator が失敗することを確認する。
template: branching_invalid
scenario_name: museum_heist

goal:
  clear_condition: |
    指紋と目撃証言が両方揃えば逮捕できる
  fail_condition: |
    どちらか一方でも欠けていたら釈放になる

branches:
  - id: fingerprint
    label: 「指紋」カード
    location: display_case
  - id: testimony
    label: 「目撃証言」カード
    location: security_desk

check_type: all_of

scenes:
  start:          investigation
  terminal_clear: arrest_scene
  terminal_fail:  release_scene
