# event_driven_basic spec fixture
# scenarios/generated/event_driven_basic_expected_output.yaml と同じ出力を生成する spec。
# 最小構成（billiards_clear）
template: event_driven_basic
scenario_name: billiards_clear

setup:
  shot_limit:   2
  target_count: 1

boot_records:
  - shot_committed
  - ball_pocketed

scenes:
  terminal_clear: victory
  terminal_fail:  defeat
